global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'simple-node-app'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:3000'] # Points to the Docker service name 'app'